namespace Commun
{
    public class Enums
    {
        public enum Estado
        {
            Inactivo = 0,
            Activo = 1,
            Anulado = 2
        }

        public enum EstadoClave
        {
            Bloqueado = 0,
            CambioAdmin = 1,
            CambioUsuario = 2
        }
    }
}
